<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="order_form_inherit" model="ir.ui.view">
        <field name="name">sale.order.form.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='payment_term_id']" position="after">
                <field name="allowed_product_ids" widget="many2many_tags" invisible="1"/>
                <field name="allowed_template_ids" widget="many2many_tags" invisible="1"/>
            </xpath>

            <xpath expr="//page[@name='order_lines']/field[@name='order_line']/list/field[@name='product_template_id']" position="attributes">
                <attribute name="domain">[('sale_ok', '=', True), ('id', 'in', parent.allowed_template_ids)]</attribute>
            </xpath>
            <xpath expr="//page[@name='order_lines']/field[@name='order_line']/list/field[@name='product_id']" position="attributes">
                <attribute name="domain">[('sale_ok', '=', True), ('id', 'in', parent.allowed_product_ids)]</attribute>
			</xpath>
        </field>
    </record>

</odoo>
